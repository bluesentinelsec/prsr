# Publish prsr to PyPI when a GitHub Release is published.
#
# Idiomatic flow:
#   1. Bump src/prsr/_version.py (and CHANGELOG)
#   2. Merge to main
#   3. Create a GitHub Release with tag vX.Y.Z (must match package version)
#   4. This workflow builds sdist/wheel and uploads via Trusted Publishing (OIDC)
#
# Prerequisites (one-time, before first real publish):
#   - PyPI project "prsr" with Trusted Publisher:
#       Owner: bluesentinelsec, Repository: prsr,
#       Workflow: publish.yml, Environment: pypi
#   - Optional TestPyPI trusted publisher with Environment: testpypi
#   - GitHub Environments "pypi" / "testpypi" (optional protection rules)
name: Publish

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      target:
        description: "Publish target (use testpypi until ready for real PyPI)"
        type: choice
        options:
          - testpypi
          - pypi
        default: testpypi

permissions:
  contents: read

concurrency:
  group: publish-${{ github.event_name }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  build:
    name: Build distributions
    runs-on: ubuntu-latest
    outputs:
      package_version: ${{ steps.meta.outputs.package_version }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install build tooling
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e ".[dev]"

      - name: Read package version
        id: meta
        run: |
          set -euo pipefail
          version="$(python -c 'from prsr._version import __version__; print(__version__)')"
          echo "package_version=${version}" >> "${GITHUB_OUTPUT}"
          echo "Package version: ${version}"

      - name: Verify release tag matches package version
        if: github.event_name == 'release'
        env:
          TAG_NAME: ${{ github.event.release.tag_name }}
          PACKAGE_VERSION: ${{ steps.meta.outputs.package_version }}
        run: |
          set -euo pipefail
          tag="${TAG_NAME#v}"
          tag="${tag#V}"
          if [ "${tag}" != "${PACKAGE_VERSION}" ]; then
            echo "Release tag '${TAG_NAME}' (normalized '${tag}') does not match"
            echo "package version '${PACKAGE_VERSION}' from src/prsr/_version.py"
            echo "Bump _version.py and CHANGELOG before creating the GitHub Release."
            exit 1
          fi
          echo "Tag and package version match: ${PACKAGE_VERSION}"

      - name: Build sdist and wheel
        run: python -m build

      - name: Twine check
        run: python -m twine check dist/*

      - name: List artifacts
        run: ls -la dist/

      - name: Upload distributions
        uses: actions/upload-artifact@v4
        with:
          name: python-package-distributions
          path: dist/
          if-no-files-found: error
          retention-days: 14

  publish-pypi:
    name: Publish to PyPI
    needs: build
    if: >
      github.event_name == 'release'
      || (github.event_name == 'workflow_dispatch' && inputs.target == 'pypi')
    runs-on: ubuntu-latest
    environment:
      name: pypi
      url: https://pypi.org/p/prsr
    permissions:
      id-token: write
    steps:
      - name: Download distributions
        uses: actions/download-artifact@v4
        with:
          name: python-package-distributions
          path: dist/

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: dist/
          print-hash: true

  publish-testpypi:
    name: Publish to TestPyPI
    needs: build
    if: github.event_name == 'workflow_dispatch' && inputs.target == 'testpypi'
    runs-on: ubuntu-latest
    environment:
      name: testpypi
      url: https://test.pypi.org/p/prsr
    permissions:
      id-token: write
    steps:
      - name: Download distributions
        uses: actions/download-artifact@v4
        with:
          name: python-package-distributions
          path: dist/

      - name: Publish to TestPyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: dist/
          repository-url: https://test.pypi.org/legacy/
          print-hash: true
